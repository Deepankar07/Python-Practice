# Creating a tuple using ()
t = (1, 2, 3, 4)

# t1 = ()   # Empty Tuple
# t1 = (1)  # Wrong way to declare a tuple with single element
t1 = (1,)  # Right way to declare a tuple with a single element
print(t1)

# Printing the elements of a tuple
# print(t[0])

# t[0] = 34 # Throws an error  # We can't update the values of a tuple